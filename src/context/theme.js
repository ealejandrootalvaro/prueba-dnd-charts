import React from 'react';

export const backgroundColor = '#f5faff';
export const primaryColor = '#deeeff';

const theme = React.createContext({ backgroundColor, primaryColor });

export default theme;
